<!DOCTYPE html>
<html>
    <head>
        <meta charset ="UTF-8">
        <title><%= judgeName %></title>
        <link rel = 'stylesheet' href='/stylesheets/style.css'/>
    </head>
    <body>
        <% if ( status == "Success") { %>
            <h1><%= status %></h1>
                <p>Judge has been added!!
                </p>
        <% }  else { %>
 
            <h1><%= status %></h1>
                <p>Judge has not been added.</p>
          <% } %>
    </body>
</html>
